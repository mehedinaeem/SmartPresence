[pytest]
testpaths = tests
pythonpath = .
markers =
    unit: lightweight checks without downloaded models
    integration: requires external assets or integrated services
    slow: expensive model or dataset operations
